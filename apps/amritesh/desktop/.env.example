# Copy to .env and fill in. .env is gitignored, never commit real values.
# Read at build time by src-tauri/build.rs; the app won't compile without them.

# Google Cloud → Credentials → OAuth client (type: Desktop app), Calendar API enabled
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
